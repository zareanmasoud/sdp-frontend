(function () {
    'use strict';

    angular.module('SdpApp').controller('NotificationController', ['$scope', function () {

        // $scope.markAsRead = function (notify) {
        //     // notificationService.markAsRead(notify);
        // };
        //
        // $scope.notifications = [];
        //
        // var updateNotification = function () {
        //     // $scope.notifications = notificationService.notifications;
        //
        //     $scope.notifications.forEach(function (notify) {
        //         moment.locale('fa');
        //         notify.created_at_txt = moment(notify.created_at, 'YYYY-MM-DDThh:mm:ss').fromNow();
        //     });
        //     $scope.$apply();
        // };
        //
        // // notificationService.register(updateNotification);
    }]);
}());
